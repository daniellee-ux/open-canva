import type { OpencanvaConfig } from '@opencanva/core';

const config: OpencanvaConfig = {
  // Defaults are fine for the demo. Examples:
  // designsDir: 'designs',
  // artboard: { w: 1080, h: 1080 },
};

export default config;
